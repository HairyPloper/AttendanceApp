# name: Deploy to GitHub Pages

# on:
#   pull_request:
#     types: [closed]
#     branches: [ main ]

# jobs:
#   build-and-deploy:
#     if: ${{ github.event.pull_request.merged == true }}
#     runs-on: windows-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'

#       - name: Install dependencies
#         run: npm ci

#       - name: Build web export
#         run: npx expo export -p web --clear

#       - name: Ensure .nojekyll exists
#         run: |
#           New-Item -Path dist/.nojekyll -ItemType File -Force

#       - name: Ensure 404 exists
#         run: |
#           Copy-Item -Path dist/index.html -Destination dist/404.html -Force

#       - name: Configure Git user for gh-pages
#         run: |
#           git config --global user.email "${{ github.actor }}@users.noreply.github.com"
#           git config --global user.name "${{ github.actor }}"

#       - name: Deploy to gh-pages
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         run: |
#           npx gh-pages -d dist --dotfiles --repo "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git"
